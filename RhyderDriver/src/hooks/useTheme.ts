import theme from "../theme/theme";

const useTheme = () => {
//   const scheme = useColorScheme();
// scheme === "dark" ? theme.darkTheme : theme.lightTheme;
  return theme.lightTheme;
};

export default useTheme;
